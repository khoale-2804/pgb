// Code generated by pgb. DO NOT EDIT.
// versions:
//   pgb v0.1.0
//   sqlc v1.31.1

package edge

import (
	"context"
	"errors"
	"github.com/jackc/pgx/v5"
	"github.com/jackc/pgx/v5/pgtype"
	pgb "github.com/khoale-2804/pgb/core"
)

// WeirdPkFilter narrows ListWeirdPks and CountWeirdPks:
// nil / zero fields are ignored; Extra composes raw predicates.
type WeirdPkFilter struct {
	ID         *int32
	IDIn       []int32
	IDGt       *int32
	IDLt       *int32
	IDGte      *int32
	IDLte      *int32
	Code       *string
	CodeIn     []string
	CodeLike   *string
	CodeILike  *string
	Label      *pgtype.Text
	LabelIn    []pgtype.Text
	LabelLike  *string
	LabelILike *string
	Extra      []pgb.Expr
}

// weirdPksFilterWhere compiles f into the ANDed predicate list; an empty
// filter yields an empty list (no WHERE).
func weirdPksFilterWhere(f WeirdPkFilter) []pgb.Expr {
	var w []pgb.Expr
	if f.ID != nil {
		w = append(w, WeirdPks.ID().Eq(*f.ID))
	}
	if len(f.IDIn) > 0 {
		w = append(w, WeirdPks.ID().In(f.IDIn...))
	}
	if f.IDGt != nil {
		w = append(w, WeirdPks.ID().Gt(*f.IDGt))
	}
	if f.IDLt != nil {
		w = append(w, WeirdPks.ID().Lt(*f.IDLt))
	}
	if f.IDGte != nil {
		w = append(w, WeirdPks.ID().Gte(*f.IDGte))
	}
	if f.IDLte != nil {
		w = append(w, WeirdPks.ID().Lte(*f.IDLte))
	}
	if f.Code != nil {
		w = append(w, WeirdPks.Code().Eq(*f.Code))
	}
	if len(f.CodeIn) > 0 {
		w = append(w, WeirdPks.Code().In(f.CodeIn...))
	}
	if f.CodeLike != nil {
		w = append(w, WeirdPks.Code().Like(*f.CodeLike))
	}
	if f.CodeILike != nil {
		w = append(w, WeirdPks.Code().ILike(*f.CodeILike))
	}
	if f.Label != nil {
		w = append(w, WeirdPks.Label().Eq(*f.Label))
	}
	if len(f.LabelIn) > 0 {
		w = append(w, WeirdPks.Label().In(f.LabelIn...))
	}
	if f.LabelLike != nil {
		w = append(w, WeirdPks.Label().Like(*f.LabelLike))
	}
	if f.LabelILike != nil {
		w = append(w, WeirdPks.Label().ILike(*f.LabelILike))
	}
	w = append(w, f.Extra...)
	return w
}

// WeirdPkSet is the three-state UPDATE payload: a zero Set field
// skips its column, SetOf writes a value, SetNull writes NULL.
type WeirdPkSet struct {
	ID    Set[int32]
	Label Set[pgtype.Text]
}

// InsertWeirdPkParams carries the INSERT column values: generated
// and serial columns are out, default-bearing columns join only with
// the include_defaults option.
type InsertWeirdPkParams struct {
	ID    int32
	Code  string
	Label pgtype.Text
}

// scanWeirdPk scans one row positionally over every column in
// catalog order — the single scan path for all generated reads.
func scanWeirdPk(row pgx.CollectableRow) (WeirdPk, error) {
	var m WeirdPk
	if err := row.Scan(&m.ID, &m.Code, &m.Label); err != nil {
		return WeirdPk{}, err
	}
	return m, nil
}

// GetWeirdPk returns one row by code; pgb.ErrNotFound when absent
// (pgx.ErrNoRows mapped — errors.Is keeps working for both).
func GetWeirdPk(ctx context.Context, exec pgb.DBTX, code string) (WeirdPk, error) {
	sql, args := WeirdPks.Select().Where(WeirdPks.Code().Eq(code)).SQL()
	var m WeirdPk
	err := exec.QueryRow(ctx, sql, args...).Scan(&m.ID, &m.Code, &m.Label)
	if err != nil {
		if errors.Is(err, pgx.ErrNoRows) {
			return WeirdPk{}, pgb.ErrNotFound
		}
		return WeirdPk{}, err
	}
	return m, nil
}

// ListWeirdPks returns the rows matching f; limit <= 0 means no LIMIT.
func ListWeirdPks(ctx context.Context, exec pgb.DBTX, f WeirdPkFilter, opts ...pgb.ListOpt) ([]WeirdPk, error) {
	sel := WeirdPks.Select().Where(weirdPksFilterWhere(f)...).ApplyList(opts...)
	rows, err := sel.Run(ctx, exec)
	if err != nil {
		return nil, err
	}
	return pgx.CollectRows(rows, scanWeirdPk)
}

// CountWeirdPks counts the rows matching f.
func CountWeirdPks(ctx context.Context, exec pgb.DBTX, f WeirdPkFilter) (int64, error) {
	sql, args := pgb.NewSelect("public.weird_pk", pgb.Raw{SQL: "count(*)"}).Where(weirdPksFilterWhere(f)...).SQL()
	var n int64
	if err := exec.QueryRow(ctx, sql, args...).Scan(&n); err != nil {
		return 0, err
	}
	return n, nil
}

// InsertWeirdPk inserts one row and returns it (RETURNING every
// column, defaults included).
func InsertWeirdPk(ctx context.Context, exec pgb.DBTX, p InsertWeirdPkParams) (WeirdPk, error) {
	rows, err := pgb.NewInsert("public.weird_pk",
		[]string{"id", "code", "label"},
		[]pgb.Expr{pgb.Lit{V: p.ID}, pgb.Lit{V: p.Code}, pgb.Lit{V: p.Label}},
	).Returning(pgb.Col{Table: "weird_pk", Name: "id"}, pgb.Col{Table: "weird_pk", Name: "code"}, pgb.Col{Table: "weird_pk", Name: "label"}).Run(ctx, exec)
	if err != nil {
		return WeirdPk{}, err
	}
	us, err := pgx.CollectRows(rows, scanWeirdPk)
	if err != nil {
		return WeirdPk{}, err
	}
	if len(us) == 0 {
		return WeirdPk{}, pgb.ErrNotFound
	}
	return us[0], nil
}

const insertWeirdPksSQL = "INSERT INTO public.weird_pk (id, code, label) SELECT * FROM unnest($1::int4[], $2::text[], $3::text[]) RETURNING id, code, label"

// InsertWeirdPks inserts a whole batch in one round trip via
// unnest and returns every inserted row.
func InsertWeirdPks(ctx context.Context, exec pgb.DBTX, ps []InsertWeirdPkParams) ([]WeirdPk, error) {
	if len(ps) == 0 {
		return nil, nil
	}
	colID := make([]int32, len(ps))
	colCode := make([]string, len(ps))
	colLabel := make([]pgtype.Text, len(ps))
	for i, p := range ps {
		colID[i] = p.ID
		colCode[i] = p.Code
		colLabel[i] = p.Label
	}
	args := make([]any, 0, 3*len(ps))
	args = append(args, colID, colCode, colLabel)
	rows, err := exec.Query(ctx, insertWeirdPksSQL, args...)
	if err != nil {
		return nil, err
	}
	return pgx.CollectRows(rows, scanWeirdPk)
}

// UpdateWeirdPk applies the non-zero fields of s to one row and
// returns the updated row; pgb.ErrNotFound when absent.
func UpdateWeirdPk(ctx context.Context, exec pgb.DBTX, code string, s WeirdPkSet) (WeirdPk, error) {
	u := WeirdPks.Update()
	n := 0
	if s.ID.Valid {
		n++
		if s.ID.Null {
			u.Set("id", pgb.Lit{V: nil})
		} else {
			u.Set("id", pgb.Lit{V: s.ID.V})
		}
	}
	if s.Label.Valid {
		n++
		if s.Label.Null {
			u.Set("label", pgb.Lit{V: nil})
		} else {
			u.Set("label", pgb.Lit{V: s.Label.V})
		}
	}
	if n == 0 {
		return GetWeirdPk(ctx, exec, code)
	}
	u.Where(WeirdPks.Code().Eq(code)).Returning(pgb.Col{Table: "weird_pk", Name: "id"}, pgb.Col{Table: "weird_pk", Name: "code"}, pgb.Col{Table: "weird_pk", Name: "label"})
	rows, err := u.Run(ctx, exec)
	if err != nil {
		return WeirdPk{}, err
	}
	us, err := pgx.CollectRows(rows, scanWeirdPk)
	if err != nil {
		return WeirdPk{}, err
	}
	if len(us) == 0 {
		return WeirdPk{}, pgb.ErrNotFound
	}
	return us[0], nil
}

// UpdateWeirdPks applies s to every row matching where and
// returns the affected count; an empty where is refused with
// pgb.ErrNoWhere before any SQL is sent.
func UpdateWeirdPks(ctx context.Context, exec pgb.DBTX, where []pgb.Expr, s WeirdPkSet) (int64, error) {
	u := WeirdPks.Update()
	n := 0
	if s.ID.Valid {
		n++
		if s.ID.Null {
			u.Set("id", pgb.Lit{V: nil})
		} else {
			u.Set("id", pgb.Lit{V: s.ID.V})
		}
	}
	if s.Label.Valid {
		n++
		if s.Label.Null {
			u.Set("label", pgb.Lit{V: nil})
		} else {
			u.Set("label", pgb.Lit{V: s.Label.V})
		}
	}
	if n == 0 {
		return 0, nil
	}
	if len(where) == 0 {
		return 0, pgb.ErrNoWhere
	}
	u.Where(where...)
	tag, err := u.Exec(ctx, exec)
	if err != nil {
		return 0, err
	}
	return tag.RowsAffected(), nil
}

// UpsertWeirdPk inserts p under code, or on conflict updates
// every settable column from the proposed row (EXCLUDED.*) and returns
// the resulting row; pgb.ErrNotFound when DO NOTHING matched.
func UpsertWeirdPk(ctx context.Context, exec pgb.DBTX, code string, p InsertWeirdPkParams) (WeirdPk, error) {
	rows, err := pgb.NewInsert("public.weird_pk",
		[]string{"code", "id", "label"},
		[]pgb.Expr{pgb.Lit{V: code}, pgb.Lit{V: p.ID}, pgb.Lit{V: p.Label}},
	).OnConflict(pgb.OnConflict{
		Target: []string{"code"},
		Sets: []pgb.SetClause{
			{Col: "id", E: pgb.Col{Table: "excluded", Name: "id"}},
			{Col: "label", E: pgb.Col{Table: "excluded", Name: "label"}},
		},
	}).Returning(pgb.Col{Table: "weird_pk", Name: "id"}, pgb.Col{Table: "weird_pk", Name: "code"}, pgb.Col{Table: "weird_pk", Name: "label"}).Run(ctx, exec)
	if err != nil {
		return WeirdPk{}, err
	}
	us, err := pgx.CollectRows(rows, scanWeirdPk)
	if err != nil {
		return WeirdPk{}, err
	}
	if len(us) == 0 {
		return WeirdPk{}, pgb.ErrNotFound
	}
	return us[0], nil
}

// DeleteWeirdPk removes one row by code.
func DeleteWeirdPk(ctx context.Context, exec pgb.DBTX, code string) error {
	_, err := WeirdPks.Delete().Where(WeirdPks.Code().Eq(code)).Exec(ctx, exec)
	return err
}

// DeleteWeirdPks removes every row matching where and returns the
// affected count; an empty where is refused with pgb.ErrNoWhere.
func DeleteWeirdPks(ctx context.Context, exec pgb.DBTX, where []pgb.Expr) (int64, error) {
	if len(where) == 0 {
		return 0, pgb.ErrNoWhere
	}
	tag, err := WeirdPks.Delete().Where(where...).Exec(ctx, exec)
	if err != nil {
		return 0, err
	}
	return tag.RowsAffected(), nil
}
